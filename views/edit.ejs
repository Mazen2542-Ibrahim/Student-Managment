<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Student Management</title>

    <style>
        body {
            background-color: #e6e6e6;
        }
    </style>

</head>

<body>

    <div class="container  mt-5">
        <h2 class="card shadow p-2 mt-3 text-center">Edit Student information</h2>
    </div>

    <div class="container mt-3">
        <form class="card shadow rounded-3 p-3 d-flex gap-2" action="/edit/<%= student._id %>?_method=PUT"
            method="POST">
            <div>
                <label class="form-label" for="firstname">First Name</label>
                <input class="form-control" required value="<%= student.firstname %>" name="firstname" type="text"
                    id="firstname">
            </div>
            <div>
                <label class="form-label" for="lastname">Last Name</label>
                <input class="form-control" required value="<%= student.lastname %>" name="lastname" type="text"
                    id="lastname">
            </div>
            <div>
                <label class="form-label" for="age">Age</label>
                <input class="form-control" required value="<%= student.age %>" name="age" type="number" id="age">
            </div>
            <div class="btn-group-sm">
                <button class="btn btn-success mt-2" type="submit">Save</button>
                <a href="/" class="btn btn-outline-secondary mt-2">Cancel</a>
            </div>
        </form>
    </div>

</body>

</html>